import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    button: {
        width: 150,
        margin: 10,
        backgroundColor: '#215f00'
    },
    textButton: {
        color: '#000'
    },
    title: {
        fontWeight: 'bold',
        fontSize: 25
    },
    scrollContainer: {
        flexGrow: 1,
        backgroundColor: '#F5FCFF',
        // justifyContent: 'center',
        alignItems: 'center',
        paddingTop: 10
    }
});

export default styles;
